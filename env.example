# Environment Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Redis Configuration
REDIS_SECRET=your-redis-secret-key-here

# Session Configuration
SESSION_SECRET=your-session-secret-key-here-minimum-32-characters

# Database Configuration
DB_HOST=localhost
DB_USER=your-db-user
DB_PASS=your-db-password
DB_NAME=your-db-name

# SSL Configuration (for production)
# Encode your SSL certificates in base64 and set these variables
# SSL_KEY=base64-encoded-private-key
# SSL_CERT=base64-encoded-certificate
